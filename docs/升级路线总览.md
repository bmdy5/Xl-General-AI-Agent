# MyAgent 升级路线总览

> 原则：**能复用优先复用，简单优先，每阶段可独立验证**

---

## 当前状态（阶段 1-4 全部完成 + 自学/进化/网关/子代理）

```
✅ 核心循环（while-true + AsyncGenerator，200 步）
✅ 9 个工具（read_file/write_file/bash/web_search/web_fetch/read_image/image2/spawn_agent/save_memory）
✅ JSONL 会话持久化 + Transcript repair
✅ MEMORY.md 自动记忆 + 5 类记忆 + 用户画像
✅ 上下文压缩（Head/Tail + LLM 摘要 + 熔断器）
✅ CLI 交互 + Plan Mode + QQ Gateway
✅ 自主学习（子代理并行 + 辩论审查）
✅ 自进化 7 模式（审计/反思/规则/技能追踪等）
✅ 知识库清理（pro 模型批量审查）
✅ Sub-agent 委派（5 角色，3 层深度限制）
✅ DeepSeek V4 Pro/Flash 双模型
```

## 升级路线（已全部完成）

```
阶段 2: 工具系统    阶段 3: 上下文压缩   阶段 4: 自动记忆
   ✅ 已完成            ✅ 已完成             ✅ 已完成
      │                   │                    │
      ▼                   ▼                    ▼
  9 个工具上线        熔断器+断点保护      save_memory + 记忆注入
```

### 下一阶段：与同事 Agent 对比后的升级方向

详见 `对比分析-与同事Agent对比.md`，高优先：

1. **grep_search + list_directory 工具** → 补齐基础工具缺口
2. **权限系统（三级）** → CLI 和 Gateway 工具安全
3. **写后验证** → file_write 语法检查
4. **tiktoken 精确计量** → 压缩时机更准
5. **工具失败自省** → 减少死循环

### 依赖关系

```
阶段 2（web_search）   → 独立，不依赖其他阶段
阶段 3（上下文压缩）    → 独立，但建议在阶段 2 之后（agent 更完整了再优化）
阶段 4（自动记忆）      → 依赖阶段 1 的 MemoryManager，与阶段 3 配合使用
```

### 每阶段的"做完标志"

| 阶段 | 验证方式 |
|------|---------|
| 2 | `./run.sh "搜索一下今天有什么科技新闻"` → 返回真实搜索结果 |
| 3 | 连续聊 40 轮不报错，早期对话被自动压缩为摘要 |
| 4 | 告诉 agent 一条偏好 → 退出重启 → 问它"我的偏好是什么？" → 正确回答 |

---

## 各阶段代码来源总表

| 阶段 | 文件 | 抄了什么 | 改了/简化了什么 |
|------|------|---------|---------------|
| 2 | `web_search_tool.py` | tinypace 的 BaseTool 接口 + 格式化逻辑 | 底层从 BS4 HTML 抓取换成 duckduckgo_search 库 |
| 3 | `compressor.py` | tinypace 的 Head/Tail 分割 + LLM 摘要模式 | 去掉 tiktoken/FileRecovery/Phase2/索引等耦合，加熔断器 |
| 4 | `memory_tool.py` | hermes 的 action 参数模式（add/replace/remove/read） | 底层接已有的 MemoryManager，去文件锁/安全扫描等 |

## 暂时不做的

| 功能 | 为什么不做 |
|------|-----------|
| **MCP 集成** | MCP 需要独立 server，目前工具够用 |
| **Computer Use** | 需要 GUI 环境，与 CLI agent 定位不同 |
| **ChromaDB 向量搜索** | 需要 embedding 模型+向量库，当前记忆量几十条够用，上百条后再做 |
